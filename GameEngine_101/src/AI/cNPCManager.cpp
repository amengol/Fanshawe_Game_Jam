#include "cNPCManager.h"


cNPCManager::cNPCManager()
{
}


cNPCManager::~cNPCManager()
{
}

void cNPCManager::Evaluate()
{
    //std::string error;
    //for (size_t i = 0; i < mNPCs.size(); i++)
    //{
    //    g_characterControl.SetControlledCharacter(mNPCs[i]->friendlyName, error);
    //    g_characterControl.Forward();

    //    // Make it turn to the player
    //    cGameObject* npc = mNPCs[i];
    //    glm::vec3 velocity = glm::normalize(mPlayer->position - npc->position) * 1.5f;


    //    //npc->rigidBody->rotateY(1.0f);
    //    npc->rigidBody->SetVelocity(velocity);
    //}
}
